class User {
  constructor(params) {
    this.id = params.id;
    this.userName = params.userName;
    this.passWord = params.passWord;
    this.userProfileId = params.userProfileId;
  }
}

export default User;
